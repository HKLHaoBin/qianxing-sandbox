# AGENTS.md — 千星沙箱

人跟 AI 说事情，AI 读仓库里的项目文档和记忆后再答。不要让协作者自己去 GitHub 翻文档。

## 时机到了就读 skill（不要等用户提醒）

下面任一情况出现，**先读对应 skill 再回答或动手**。没读 skill 就不算开始。时机已经到了就做完，不要反问「要不要提交 / 要不要记下来」。

1. 用户在描述想做的游戏、玩法或关卡，或说从原件开始、写伪代码、列出全部原件、做分工 → `.cursor/skills/miliastra-brief/SKILL.md`
2. 用户要某个元件或节点图的信息、说自己负责某元件，或问某张节点图里某个节点出了什么问题、该怎么处理 → `.cursor/skills/miliastra-project/SKILL.md`
3. 节点图做完、用户确认资产文件无误、用户建了某个游戏的资产文件夹并要上传，或确认过的资产已经可以进仓库 → `.cursor/skills/miliastra-sync/SKILL.md`
4. 指导用户在编辑器里改节点图 → `.cursor/skills/miliastra-node-edit/SKILL.md`
5. 查节点含义、官方用法、排障 → `.cursor/skills/miliastra-knowledge/SKILL.md`
6. 用户提到 Lua、客户端脚本、客户端控件、引用控件、控件模板，或玩法主要是屏幕 UI / 2D 交互（扇形手牌、自定义 HUD、界面动画等）→ 先读 `.cursor/skills/miliastra-lua/SKILL.md`，再读 `.cursor/skills/miliastra-beyond/SKILL.md`（此类表现优先 Lua；**开发与自测优先走千星沙箱模拟器**；权威逻辑仍用服务端节点图）。写或修脚本、查 API/模板时，按 `miliastra-lua/references/authoring-pack.md` 打开仓库内 **客户端脚本需求档案 v3.1**（入口 `千星奇域Lua开发环境-客户端脚本需求档案-v3.1/AGENT.md`），只加载当前任务分册
7. 用户提到模拟器、beyond、试玩存档、Authoring JSON、模拟器 MCP/Web，或已在做 Lua 需要装工具/试玩/截图 → `.cursor/skills/miliastra-beyond/SKILL.md`

动手前先读 `memory-graph.md` 和 `memory/lessons/`。本机若有 `memory/local/`，一并读取；那个目录不上传。

规划新玩法前：先看仓库里已有的常用资产（根目录两份 `.gia`）和本地资产中心，能直接导入的复合节点或数据结构优先导入。屏幕 UI / 2D 表现对照 `文档/客户端Lua与UI.md`，能 Lua 的不要先拆成物件节点图；落地时优先用 `文档/千星沙箱模拟器.md` 所述工具链，再导出真机验证。写码细则与教学模板在 `千星奇域Lua开发环境-客户端脚本需求档案-v3.1/`。

## 共享边界

可进 GitHub 的：工具、技能、可复用文档、经验、记忆、根目录这两份资产，以及 **客户端脚本需求档案**（`千星奇域Lua开发环境-客户端脚本需求档案-v3.1/`）：

- `常用复合节点大全v1.7(补充包同步更新中).gia`
- `数据结构：队列-二维数组-栈.gia`

`门限夺宝/`、`门线夺宝/`、`文档/限时夺宝-时间门/`、`memory/local/`、`beyond-workspace/`、`tools/miliastra-beyond-simulator/` 保持在本机，不要 `git add`。

## 节点图编辑输出格式（强制）

只要指导用户去编辑《原神》千星沙箱节点图，**每一条改动**都必须同时给出以下项，**缺一不可**：

1. **元件名称**（含所在节点图名称，写成：`元件「…」/ 节点图「…」`）
2. **节点卡名称**（编辑器中的中文节点名）
3. **搜索 ID**（节点卡 index；可从导出 GIA 的 `Index` 读取；同名卡靠 ID 区分）
4. **字段名**（该节点卡上的参数/引脚中文名）
5. **参数**（手填的具体值，或明确的接线来源：`← 某节点卡(ID) · 某字段`）

缺任何一项都不算完成说明。禁止只写「改成 ≥1」「关掉选项卡」这类不含节点卡/字段/参数的模糊说法。

**新建节点卡的 ID**：编辑器会**先占用空号（曾删除/未用的 index）**，空号用尽后才 `最高 ID+1` 递增。推算时先根据导出列出空号，按创建顺序分配「最小空号 → … → 最高+1」。说明里直接写推算 ID；导出与推算冲突时以导出为准。

**节点中文名**：以用户编辑器能搜到的为准（例：`列表迭代循环`、`设置界面控件(组)状态`）；勿照搬知识库旧称。易踩坑见 `memory/lessons/` 与 skill。

**只写需要改的**：未改动、保持原样的节点卡/字段/接线一律不要列出。

涉及执行流时：写明逻辑入/逻辑出接到哪张节点卡（含 ID）的哪个脚；终端出脚写「不接」。
「设置自定义变量」须写清：目标实体、变量名、变量值、是否触发事件。不要把节点卡误称为节点图。

权威细则与模板见：`.cursor/skills/miliastra-node-edit/SKILL.md`。

搜索 ID 以项目文档或导出为准。文档和导出都没有的 ID，写「文档未记录」，不要编。
