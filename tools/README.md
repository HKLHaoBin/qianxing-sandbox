# 本仓库自带的 GIA 脚本

`tools/*.ts`、`tools/*.py`、`tools/*.mjs` 用来解码、对照和列出本地 `.gia`。节点语义和编辑器包不在这些脚本里：

- 编辑器包：`tools/Miliastra-Node-Editor-Pack`（`scripts/setup.ps1` 克隆）
- 知识库：`文档/Miliastra-knowledge`（同一条 setup）
- 查询用法：`.cursor/skills/miliastra-knowledge/SKILL.md`

脚本里如果出现某个私人玩法的路径，那个玩法的资产仍然不在本仓库中。
