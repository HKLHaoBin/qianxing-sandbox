# Beyond Simulator 工具（本仓库侧）

上游：[miliastra-beyond-simulator](https://github.com/1475505/miliastra-beyond-simulator)。

本目录只放**接入本方法论仓库**所需的配置示例，不嵌模拟器源码。

| 文件 | 说明 |
|------|------|
| `mcp.json.example` | Cursor MCP 配置片段；把 workspace 换成你的绝对路径 |
| `../scripts/setup.ps1` / `setup.sh` | 安装 `beyond-simulator-web` / `beyond-simulator-mcp` 并创建 `beyond-workspace/` |

Agent 步骤见 `.cursor/skills/miliastra-beyond/SKILL.md`。人读总览见 `文档/千星沙箱模拟器.md`。
